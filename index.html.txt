<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NNRG Model United Nations 2026 | International Conference</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700;900&family=Cormorant+Garamond:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* ===== GLOBAL RESET & VARIABLES ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            /* BLACK & GOLD THEME */
            --primary-black: #0A0A0A;
            --secondary-black: #1A1A1A;
            --tertiary-black: #2A2A2A;
            --dark-gray: #3A3A3A;
            --light-gray: #8A8A8A;
            --off-white: #F5F5F0;
            --pure-white: #FFFFFF;
            
            /* Gold Palette */
            --gold-primary: #D4AF37;
            --gold-light: #F4D03F;
            --gold-dark: #B8860B;
            --gold-muted: #C5A028;
            --gold-shine: #FFD700;
            
            /* Typography */
            --font-display: 'Playfair Display', serif;
            --font-elegant: 'Cormorant Garamond', serif;
            --font-body: 'Inter', sans-serif;
            
            /* Spacing */
            --section-padding: 140px 8%;
            --mobile-padding: 80px 5%;
        }

        body {
            font-family: var(--font-body);
            background: var(--primary-black);
            color: var(--off-white);
            overflow-x: hidden;
            line-height: 1.6;
        }

        html {
            scroll-behavior: smooth;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: var(--primary-black);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--gold-primary);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--gold-light);
        }

        /* ===== PRELOADER ===== */
        .preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--primary-black);
            z-index: 10000;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            transition: opacity 0.8s ease, visibility 0.8s ease;
        }

        .preloader.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .preloader-logo {
            font-family: var(--font-display);
            font-size: 48px;
            font-weight: 900;
            color: var(--gold-primary);
            margin-bottom: 30px;
            animation: logoReveal 1.5s ease forwards;
            opacity: 0;
        }

        @keyframes logoReveal {
            0% { opacity: 0; transform: translateY(30px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        .preloader-bar {
            width: 200px;
            height: 2px;
            background: var(--tertiary-black);
            border-radius: 2px;
            overflow: hidden;
        }

        .preloader-progress {
            width: 0%;
            height: 100%;
            background: linear-gradient(90deg, var(--gold-dark), var(--gold-primary), var(--gold-light));
            animation: loadProgress 2s ease forwards;
        }

        @keyframes loadProgress {
            0% { width: 0%; }
            100% { width: 100%; }
        }

        /* ===== CURSOR ===== */
        .cursor {
            width: 20px;
            height: 20px;
            border: 2px solid var(--gold-primary);
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            transition: transform 0.15s ease, opacity 0.15s ease;
            transform: translate(-50%, -50%);
        }

        .cursor-dot {
            width: 6px;
            height: 6px;
            background: var(--gold-primary);
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            transform: translate(-50%, -50%);
        }

        .cursor.hover {
            transform: translate(-50%, -50%) scale(2);
            background: rgba(212, 175, 55, 0.1);
        }

        /* ===== NAVIGATION ===== */
        nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(10, 10, 10, 0.9);
            backdrop-filter: blur(20px);
            z-index: 1000;
            padding: 25px 8%;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border-bottom: 1px solid transparent;
        }

        nav.scrolled {
            padding: 15px 8%;
            background: rgba(10, 10, 10, 0.98);
            border-bottom: 1px solid rgba(212, 175, 55, 0.2);
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1600px;
            margin: 0 auto;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-emblem {
            width: 50px;
            height: 50px;
            border: 2px solid var(--gold-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .logo-emblem::before {
            content: '⚜';
            font-size: 24px;
            color: var(--gold-primary);
        }

        .logo-emblem::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(212, 175, 55, 0.3), transparent);
            transform: rotate(45deg);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%) rotate(45deg); }
            100% { transform: translateX(100%) rotate(45deg); }
        }

        .logo-text {
            font-family: var(--font-display);
            font-size: 28px;
            font-weight: 900;
            background: linear-gradient(135deg, var(--gold-light), var(--gold-primary), var(--gold-dark));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: 1px;
        }

        .nav-links {
            display: flex;
            gap: 50px;
            list-style: none;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--off-white);
            font-weight: 500;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            position: relative;
            padding: 5px 0;
        }

        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 1px;
            background: linear-gradient(90deg, var(--gold-primary), var(--gold-light));
            transition: width 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .nav-links a:hover {
            color: var(--gold-primary);
        }

        .nav-links a:hover::before {
            width: 100%;
        }

        .menu-toggle {
            display: none;
            background: none;
            border: 2px solid var(--gold-primary);
            padding: 10px 15px;
            color: var(--gold-primary);
            cursor: pointer;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
        }

        .menu-toggle:hover {
            background: var(--gold-primary);
            color: var(--primary-black);
        }

        /* Mobile Menu */
        .mobile-menu {
            position: fixed;
            top: 0;
            right: -100%;
            width: 100%;
            height: 100vh;
            background: var(--primary-black);
            z-index: 999;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 40px;
            transition: right 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .mobile-menu.active {
            right: 0;
        }

        .mobile-menu a {
            font-family: var(--font-display);
            font-size: 36px;
            color: var(--off-white);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .mobile-menu a:hover {
            color: var(--gold-primary);
        }

        .menu-close {
            position: absolute;
            top: 30px;
            right: 30px;
            background: none;
            border: none;
            font-size: 32px;
            color: var(--gold-primary);
            cursor: pointer;
        }

        /* ===== HERO SECTION ===== */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            background: var(--primary-black);
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(ellipse at 20% 50%, rgba(212, 175, 55, 0.1) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 20%, rgba(212, 175, 55, 0.08) 0%, transparent 40%),
                radial-gradient(ellipse at 60% 80%, rgba(212, 175, 55, 0.05) 0%, transparent 40%);
        }

        /* UN Emblem Watermark */
        .un-watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 600px;
            height: 600px;
            opacity: 0.03;
            pointer-events: none;
            z-index: 1;
        }

        .un-watermark svg {
            width: 100%;
            height: 100%;
            fill: var(--gold-primary);
        }

        /* Animated Grid Lines */
        .grid-lines {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(212, 175, 55, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(212, 175, 55, 0.03) 1px, transparent 1px);
            background-size: 100px 100px;
            animation: gridMove 20s linear infinite;
        }

        @keyframes gridMove {
            0% { transform: translate(0, 0); }
            100% { transform: translate(100px, 100px); }
        }

        /* Floating Particles */
        .particles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: var(--gold-primary);
            border-radius: 50%;
            opacity: 0.3;
            animation: floatParticle 15s infinite ease-in-out;
        }

        @keyframes floatParticle {
            0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.3; }
            50% { transform: translateY(-100px) rotate(180deg); opacity: 0.8; }
        }

        .hero-content {
            position: relative;
            z-index: 10;
            text-align: center;
            max-width: 1200px;
            padding: 0 20px;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 15px;
            background: rgba(212, 175, 55, 0.1);
            border: 1px solid rgba(212, 175, 55, 0.3);
            color: var(--gold-primary);
            padding: 12px 30px;
            border-radius: 50px;
            font-size: 13px;
            font-weight: 600;
            margin-bottom: 40px;
            letter-spacing: 3px;
            text-transform: uppercase;
            animation: fadeInDown 1s ease 0.5s both;
        }

        .hero-badge::before,
        .hero-badge::after {
            content: '✦';
            font-size: 10px;
        }

        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero h1 {
            font-family: var(--font-display);
            font-size: 100px;
            font-weight: 900;
            line-height: 1;
            margin-bottom: 30px;
            letter-spacing: -2px;
            animation: heroTitle 1.2s ease 0.8s both;
        }

        .hero h1 .line {
            display: block;
            overflow: hidden;
        }

        .hero h1 .line span {
            display: block;
            animation: slideUp 1s ease both;
        }

        .hero h1 .line:nth-child(1) span { animation-delay: 0.8s; }
        .hero h1 .line:nth-child(2) span { animation-delay: 1s; }

        @keyframes slideUp {
            from { transform: translateY(100%); }
            to { transform: translateY(0); }
        }

        .hero h1 .text-white {
            color: var(--off-white);
        }

        .hero h1 .text-gold {
            background: linear-gradient(135deg, var(--gold-light), var(--gold-primary), var(--gold-dark));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        @keyframes heroTitle {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-tagline {
            font-family: var(--font-elegant);
            font-size: 26px;
            color: var(--light-gray);
            margin-bottom: 50px;
            font-weight: 400;
            font-style: italic;
            animation: fadeIn 1s ease 1.2s both;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .countdown {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin: 60px 0;
            animation: fadeIn 1s ease 1.4s both;
        }

        .countdown-item {
            position: relative;
            width: 120px;
            height: 140px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: rgba(26, 26, 26, 0.8);
            border: 1px solid rgba(212, 175, 55, 0.2);
            border-radius: 10px;
            transition: all 0.4s ease;
        }

        .countdown-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.1), transparent);
            border-radius: 10px;
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .countdown-item:hover::before {
            opacity: 1;
        }

        .countdown-item:hover {
            transform: translateY(-10px);
            border-color: var(--gold-primary);
            box-shadow: 0 20px 40px rgba(212, 175, 55, 0.15);
        }

        .countdown-number {
            font-family: var(--font-display);
            font-size: 56px;
            font-weight: 900;
            background: linear-gradient(180deg, var(--gold-light), var(--gold-primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1;
        }

        .countdown-label {
            font-size: 11px;
            color: var(--light-gray);
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-top: 10px;
        }

        .hero-cta {
            display: flex;
            gap: 30px;
            justify-content: center;
            align-items: center;
            animation: fadeIn 1s ease 1.6s both;
        }

        .btn-primary {
            position: relative;
            background: linear-gradient(135deg, var(--gold-dark), var(--gold-primary));
            color: var(--primary-black);
            padding: 20px 50px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 2px;
            display: inline-flex;
            align-items: center;
            gap: 15px;
            overflow: hidden;
            transition: all 0.4s ease;
            border: none;
            cursor: pointer;
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s ease;
        }

        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(212, 175, 55, 0.4);
        }

        .scroll-indicator {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            animation: fadeIn 1s ease 2s both;
        }

        .scroll-text {
            font-size: 11px;
            color: var(--light-gray);
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .scroll-line {
            width: 1px;
            height: 60px;
            background: linear-gradient(to bottom, var(--gold-primary), transparent);
            animation: scrollPulse 2s ease-in-out infinite;
        }

        @keyframes scrollPulse {
            0%, 100% { opacity: 0.3; height: 60px; }
            50% { opacity: 1; height: 80px; }
        }

        /* ===== SECTION STYLING ===== */
        section {
            padding: var(--section-padding);
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 100px;
        }

        .section-number {
            font-family: var(--font-display);
            font-size: 120px;
            font-weight: 900;
            color: transparent;
            -webkit-text-stroke: 1px rgba(212, 175, 55, 0.2);
            position: absolute;
            top: 60px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 0;
        }

        .section-badge {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: transparent;
            color: var(--gold-primary);
            padding: 8px 0;
            font-size: 12px;
            font-weight: 600;
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 4px;
            position: relative;
        }

        .section-badge::before,
        .section-badge::after {
            content: '';
            width: 40px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold-primary));
        }

        .section-badge::after {
            background: linear-gradient(90deg, var(--gold-primary), transparent);
        }

        .section-title {
            font-family: var(--font-display);
            font-size: 64px;
            font-weight: 900;
            color: var(--off-white);
            margin-bottom: 25px;
            letter-spacing: -1px;
            line-height: 1.1;
        }

        .section-title .highlight {
            background: linear-gradient(135deg, var(--gold-light), var(--gold-primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-description {
            font-family: var(--font-elegant);
            font-size: 20px;
            color: var(--light-gray);
            max-width: 700px;
            margin: 0 auto;
            line-height: 1.8;
            font-style: italic;
        }

        /* ===== ABOUT SECTION ===== */
        .about {
            background: var(--primary-black);
            position: relative;
        }

        .about-content-wrapper {
            max-width: 900px;
            margin: 0 auto;
        }

        .about-text {
            font-size: 18px;
            line-height: 2;
            color: var(--light-gray);
            margin-bottom: 50px;
            text-align: center;
        }

        .about-text p {
            margin-bottom: 30px;
        }

        .about-features {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
            margin-bottom: 80px;
        }

        .about-feature {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            gap: 15px;
            padding: 30px 20px;
            background: rgba(26, 26, 26, 0.5);
            border: 1px solid rgba(212, 175, 55, 0.1);
            border-radius: 10px;
            transition: all 0.4s ease;
        }

        .about-feature:hover {
            border-color: var(--gold-primary);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .about-feature-icon {
            width: 60px;
            height: 60px;
            border: 1px solid var(--gold-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            flex-shrink: 0;
            transition: all 0.4s ease;
        }

        .about-feature:hover .about-feature-icon {
            background: var(--gold-primary);
            transform: rotate(360deg);
        }

        .about-feature-text h4 {
            font-size: 16px;
            font-weight: 700;
            color: var(--off-white);
            margin-bottom: 5px;
        }

        .about-feature-text p {
            font-size: 14px;
            color: var(--light-gray);
        }

        .about-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 40px;
        }

        .stat-card {
            text-align: center;
            padding: 50px 30px;
            background: rgba(26, 26, 26, 0.5);
            border: 1px solid rgba(212, 175, 55, 0.1);
            border-radius: 10px;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--gold-dark), var(--gold-primary), var(--gold-light));
            transform: scaleX(0);
            transition: transform 0.5s ease;
        }

        .stat-card:hover::before {
            transform: scaleX(1);
        }

        .stat-card:hover {
            transform: translateY(-15px);
            border-color: var(--gold-primary);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.3);
        }

        .stat-number {
            font-family: var(--font-display);
            font-size: 72px;
            font-weight: 900;
            background: linear-gradient(180deg, var(--gold-light), var(--gold-primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1;
            margin-bottom: 15px;
        }

        .stat-label {
            font-size: 14px;
            color: var(--light-gray);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* ===== WHY NNRG MUN ===== */
        .why-nnrg {
            background: var(--secondary-black);
            position: relative;
            overflow: hidden;
        }

        .why-nnrg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(ellipse at 0% 50%, rgba(212, 175, 55, 0.05) 0%, transparent 50%),
                radial-gradient(ellipse at 100% 50%, rgba(212, 175, 55, 0.05) 0%, transparent 50%);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            position: relative;
            z-index: 1;
        }

        .feature-card {
            background: rgba(10, 10, 10, 0.8);
            padding: 50px 40px;
            border: 1px solid rgba(212, 175, 55, 0.1);
            border-radius: 10px;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.1), transparent);
            opacity: 0;
            transition: opacity 0.5s ease;
        }

        .feature-card:hover::before {
            opacity: 1;
        }

        .feature-card:hover {
            transform: translateY(-15px);
            border-color: var(--gold-primary);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4);
        }

        .feature-icon {
            width: 80px;
            height: 80px;
            border: 2px solid var(--gold-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            margin-bottom: 30px;
            transition: all 0.4s ease;
        }

        .feature-card:hover .feature-icon {
            background: var(--gold-primary);
            transform: rotate(360deg);
        }

        .feature-title {
            font-family: var(--font-display);
            font-size: 24px;
            font-weight: 700;
            color: var(--off-white);
            margin-bottom: 15px;
        }

        .feature-description {
            font-size: 15px;
            color: var(--light-gray);
            line-height: 1.8;
        }

        /* ===== COMMITTEES - FLIP CARDS ===== */
        .committees {
            background: var(--primary-black);
            position: relative;
        }

        .committees-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 40px;
            perspective: 1000px;
        }

        .committee-card {
            height: 450px;
            position: relative;
            cursor: pointer;
        }

        .committee-card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
            transform-style: preserve-3d;
        }

        .committee-card:hover .committee-card-inner {
            transform: rotateY(180deg);
        }

        .committee-front,
        .committee-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 15px;
            overflow: hidden;
        }

        .committee-front {
            background: linear-gradient(180deg, var(--secondary-black), var(--tertiary-black));
            border: 2px solid rgba(212, 175, 55, 0.3);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 40px;
        }

        .committee-front::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 50% 30%, rgba(212, 175, 55, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 50% 70%, rgba(212, 175, 55, 0.1) 0%, transparent 50%);
        }

        .committee-emblem {
            width: 120px;
            height: 120px;
            border: 3px solid var(--gold-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
            animation: emblemPulse 3s ease-in-out infinite;
        }

        @keyframes emblemPulse {
            0%, 100% { box-shadow: 0 0 0 0 rgba(212, 175, 55, 0.4); }
            50% { box-shadow: 0 0 30px 10px rgba(212, 175, 55, 0.2); }
        }

        .committee-emblem-text {
            font-family: var(--font-display);
            font-size: 36px;
            font-weight: 900;
            background: linear-gradient(180deg, var(--gold-light), var(--gold-primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .committee-name {
            font-family: var(--font-display);
            font-size: 48px;
            font-weight: 900;
            background: linear-gradient(180deg, var(--gold-light), var(--gold-primary), var(--gold-dark));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 15px;
            position: relative;
            z-index: 1;
        }

        .committee-full-name {
            font-family: var(--font-elegant);
            font-size: 18px;
            color: var(--light-gray);
            font-style: italic;
            position: relative;
            z-index: 1;
        }

        .flip-indicator {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 12px;
            color: var(--light-gray);
            text-transform: uppercase;
            letter-spacing: 2px;
            opacity: 0.7;
        }

        .flip-indicator::before {
            content: '↻';
            font-size: 16px;
            color: var(--gold-primary);
        }

        .committee-back {
            background: linear-gradient(135deg, var(--gold-dark), var(--gold-primary));
            transform: rotateY(180deg);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 50px 40px;
        }

        .committee-back::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 30% 20%, rgba(0, 0, 0, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 70% 80%, rgba(0, 0, 0, 0.2) 0%, transparent 50%);
        }

        .agenda-label {
            font-size: 12px;
            color: var(--primary-black);
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 20px;
            opacity: 0.8;
            position: relative;
            z-index: 1;
        }

        .agenda-text {
            font-family: var(--font-elegant);
            font-size: 22px;
            color: var(--primary-black);
            line-height: 1.8;
            font-weight: 600;
            position: relative;
            z-index: 1;
        }

        .committee-back-footer {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 12px;
            color: var(--primary-black);
            text-transform: uppercase;
            letter-spacing: 2px;
            opacity: 0.8;
        }

        /* ===== TIMELINE ===== */
        .timeline {
            background: var(--secondary-black);
            position: relative;
        }

        .timeline-container {
            max-width: 1000px;
            margin: 0 auto;
            position: relative;
        }

        .timeline-line {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            width: 2px;
            height: 100%;
            background: linear-gradient(to bottom, var(--gold-primary), var(--gold-dark), var(--gold-primary));
        }

        .timeline-item {
            display: flex;
            margin-bottom: 80px;
            position: relative;
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .timeline-item.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .timeline-item:nth-child(odd) {
            flex-direction: row;
            padding-right: calc(50% + 50px);
        }

        .timeline-item:nth-child(even) {
            flex-direction: row-reverse;
            padding-left: calc(50% + 50px);
        }

        .timeline-content {
            background: rgba(26, 26, 26, 0.8);
            border: 1px solid rgba(212, 175, 55, 0.2);
            padding: 40px;
            border-radius: 10px;
            position: relative;
            transition: all 0.4s ease;
        }

        .timeline-content:hover {
            border-color: var(--gold-primary);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .timeline-dot {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            width: 24px;
            height: 24px;
            background: var(--gold-primary);
            border-radius: 50%;
            border: 4px solid var(--primary-black);
            box-shadow: 0 0 0 4px rgba(212, 175, 55, 0.3);
            transition: all 0.4s ease;
        }

        .timeline-item:hover .timeline-dot {
            transform: translateX(-50%) scale(1.3);
            box-shadow: 0 0 20px rgba(212, 175, 55, 0.5);
        }

        .timeline-date {
            font-size: 13px;
            color: var(--gold-primary);
            font-weight: 700;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .timeline-title {
            font-family: var(--font-display);
            font-size: 26px;
            font-weight: 700;
            color: var(--off-white);
            margin-bottom: 10px;
        }

        .timeline-description {
            font-size: 15px;
            color: var(--light-gray);
            line-height: 1.7;
        }

        /* ===== REGISTRATION ===== */
        .registration {
            background: linear-gradient(135deg, var(--gold-dark) 0%, var(--gold-primary) 50%, var(--gold-dark) 100%);
            position: relative;
            overflow: hidden;
        }

        .registration::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 0, 0, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(0, 0, 0, 0.2) 0%, transparent 50%);
        }

        .registration .section-badge {
            color: var(--primary-black);
        }

        .registration .section-badge::before,
        .registration .section-badge::after {
            background: var(--primary-black);
        }

        .registration .section-title {
            color: var(--primary-black);
        }

        .registration-container {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        .registration-form-wrapper {
            background: var(--primary-black);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5);
        }

        .form-iframe {
            width: 100%;
            height: 1200px;
            border: none;
            border-radius: 15px;
            background: var(--secondary-black);
        }

        /* ===== FOOTER ===== */
        footer {
            background: var(--primary-black);
            color: var(--off-white);
            padding: 100px 8% 40px;
            border-top: 1px solid rgba(212, 175, 55, 0.2);
        }

        .footer-content {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 60px;
            margin-bottom: 60px;
        }

        .footer-brand h3 {
            font-family: var(--font-display);
            font-size: 36px;
            font-weight: 900;
            background: linear-gradient(135deg, var(--gold-light), var(--gold-primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
        }

        .footer-brand p {
            font-size: 15px;
            line-height: 1.8;
            color: var(--light-gray);
            max-width: 350px;
        }

        .footer-section h4 {
            font-size: 16px;
            font-weight: 700;
            color: var(--gold-primary);
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 15px;
        }

        .footer-links a {
            color: var(--light-gray);
            text-decoration: none;
            font-size: 15px;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .footer-links a::before {
            content: '→';
            opacity: 0;
            transform: translateX(-10px);
            transition: all 0.3s ease;
            color: var(--gold-primary);
        }

        .footer-links a:hover {
            color: var(--gold-primary);
            padding-left: 20px;
        }

        .footer-links a:hover::before {
            opacity: 1;
            transform: translateX(0);
        }

        .footer-bottom {
            border-top: 1px solid rgba(212, 175, 55, 0.1);
            padding-top: 40px;
            text-align: center;
        }

        .footer-bottom p {
            font-size: 14px;
            color: var(--light-gray);
        }

        /* ===== MOBILE STICKY CTA ===== */
        .mobile-cta {
            display: none;
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            background: var(--primary-black);
            padding: 15px 20px;
            box-shadow: 0 -4px 30px rgba(0, 0, 0, 0.5);
            z-index: 999;
            border-top: 1px solid rgba(212, 175, 55, 0.3);
        }

        .mobile-cta .btn-primary {
            width: 100%;
            justify-content: center;
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 1200px) {
            .hero h1 {
                font-size: 72px;
            }

            .section-title {
                font-size: 48px;
            }

            .features-grid,
            .committees-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .about-features {
                grid-template-columns: repeat(2, 1fr);
            }

            .footer-content {
                grid-template-columns: 1fr 1fr;
                gap: 40px;
            }

            .un-watermark {
                width: 400px;
                height: 400px;
            }
        }

        @media (max-width: 768px) {
            :root {
                --section-padding: 80px 5%;
            }

            .cursor, .cursor-dot {
                display: none;
            }

            .nav-links {
                display: none;
            }

            .menu-toggle {
                display: block;
            }

            .hero h1 {
                font-size: 42px;
            }

            .hero-tagline {
                font-size: 18px;
            }

            .countdown {
                flex-wrap: wrap;
                gap: 15px;
            }

            .countdown-item {
                width: 80px;
                height: 100px;
            }

            .countdown-number {
                font-size: 36px;
            }

            .hero-cta {
                flex-direction: column;
            }

            .btn-primary {
                width: 100%;
                justify-content: center;
            }

            .section-title {
                font-size: 36px;
            }

            .section-number {
                font-size: 80px;
            }

            .about-features,
            .about-stats,
            .features-grid,
            .committees-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .committee-card {
                height: 400px;
            }

            .timeline-line {
                left: 20px;
            }

            .timeline-item:nth-child(odd),
            .timeline-item:nth-child(even) {
                flex-direction: row;
                padding-left: 60px;
                padding-right: 0;
            }

            .timeline-dot {
                left: 20px;
            }

            .footer-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .mobile-cta {
                display: block;
            }

            .un-watermark {
                width: 300px;
                height: 300px;
            }
        }

        /* ===== ANIMATION CLASSES ===== */
        .reveal {
            opacity: 0;
            transform: translateY(60px);
            transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .reveal-left {
            opacity: 0;
            transform: translateX(-60px);
            transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal-left.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .reveal-right {
            opacity: 0;
            transform: translateX(60px);
            transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal-right.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .stagger-1 { transition-delay: 0.1s; }
        .stagger-2 { transition-delay: 0.2s; }
        .stagger-3 { transition-delay: 0.3s; }
        .stagger-4 { transition-delay: 0.4s; }
        .stagger-5 { transition-delay: 0.5s; }
        .stagger-6 { transition-delay: 0.6s; }
    </style>
</head>
<body>
    <!-- PRELOADER -->
    <div class="preloader" id="preloader">
        <div class="preloader-logo">NNRG MUN</div>
        <div class="preloader-bar">
            <div class="preloader-progress"></div>
        </div>
    </div>

    <!-- CUSTOM CURSOR -->
    <div class="cursor" id="cursor"></div>
    <div class="cursor-dot" id="cursorDot"></div>

    <!-- MOBILE MENU -->
    <div class="mobile-menu" id="mobileMenu">
        <button class="menu-close" id="menuClose">✕</button>
        <a href="#about">About</a>
        <a href="#committees">Committees</a>
        <a href="#timeline">Timeline</a>
        <a href="#register">Register</a>
    </div>

    <!-- NAVIGATION -->
    <nav id="navbar">
        <div class="nav-container">
            <div class="logo">
                <div class="logo-emblem"></div>
                <span class="logo-text">NNRG MUN 2026</span>
            </div>
            <ul class="nav-links">
                <li><a href="#about">About</a></li>
                <li><a href="#committees">Committees</a></li>
                <li><a href="#timeline">Timeline</a></li>
                <li><a href="#register">Register</a></li>
            </ul>
            <button class="menu-toggle" id="menuToggle">Menu</button>
        </div>
    </nav>

    <!-- HERO SECTION -->
    <section class="hero">
        <div class="hero-bg">
            <div class="grid-lines"></div>
            <div class="particles" id="particles"></div>
            <!-- UN Emblem Watermark -->
            <div class="un-watermark">
                <svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
                    <!-- Globe with Latitude/Longitude Lines -->
                    <circle cx="100" cy="100" r="70" fill="none" stroke="currentColor" stroke-width="2"/>
                    <ellipse cx="100" cy="100" rx="70" ry="30" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <ellipse cx="100" cy="100" rx="70" ry="50" fill="none" stroke="currentColor" stroke-width="1"/>
                    <line x1="100" y1="30" x2="100" y2="170" stroke="currentColor" stroke-width="1.5"/>
                    <line x1="30" y1="100" x2="170" y2="100" stroke="currentColor" stroke-width="1.5"/>
                    <!-- Curved lines for longitude -->
                    <path d="M 100 30 Q 60 100 100 170" fill="none" stroke="currentColor" stroke-width="1"/>
                    <path d="M 100 30 Q 140 100 100 170" fill="none" stroke="currentColor" stroke-width="1"/>
                    <!-- Olive Branches -->
                    <path d="M 20 160 Q 40 140 50 130 Q 55 125 60 120 Q 70 110 80 105" fill="none" stroke="currentColor" stroke-width="2"/>
                    <path d="M 25 155 Q 30 150 35 148" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 35 160 Q 40 155 45 150" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 45 158 Q 50 150 55 145" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 55 155 Q 60 148 65 140" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 65 150 Q 70 142 75 135" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <!-- Right branch -->
                    <path d="M 180 160 Q 160 140 150 130 Q 145 125 140 120 Q 130 110 120 105" fill="none" stroke="currentColor" stroke-width="2"/>
                    <path d="M 175 155 Q 170 150 165 148" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 165 160 Q 160 155 155 150" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 155 158 Q 150 150 145 145" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 145 155 Q 140 148 135 140" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M 135 150 Q 130 142 125 135" fill="none" stroke="currentColor" stroke-width="1.5"/>
                    <!-- Leaves on left -->
                    <ellipse cx="28" cy="152" rx="8" ry="4" transform="rotate(-45, 28, 152)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="40" cy="155" rx="8" ry="4" transform="rotate(-40, 40, 155)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="50" cy="152" rx="8" ry="4" transform="rotate(-35, 50, 152)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="60" cy="148" rx="8" ry="4" transform="rotate(-30, 60, 148)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="70" cy="142" rx="8" ry="4" transform="rotate(-25, 70, 142)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <!-- Leaves on right -->
                    <ellipse cx="172" cy="152" rx="8" ry="4" transform="rotate(45, 172, 152)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="160" cy="155" rx="8" ry="4" transform="rotate(40, 160, 155)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="150" cy="152" rx="8" ry="4" transform="rotate(35, 150, 152)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="140" cy="148" rx="8" ry="4" transform="rotate(30, 140, 148)" fill="none" stroke="currentColor" stroke-width="1"/>
                    <ellipse cx="130" cy="142" rx="8" ry="4" transform="rotate(25, 130, 142)" fill="none" stroke="currentColor" stroke-width="1"/>
                </svg>
            </div>
        </div>
        
        <div class="hero-content">
            <div class="hero-badge">✦ March 2026 • Hyderabad, India ✦</div>
            <h1>
                <div class="line"><span class="text-white">NNRG Model</span></div>
                <div class="line"><span class="text-gold">United Nations</span></div>
            </h1>
            <p class="hero-tagline">"Shaping Tomorrow's Diplomatic Leaders Through Excellence"</p>
            
            <div class="countdown">
                <div class="countdown-item">
                    <div class="countdown-number" id="days">00</div>
                    <div class="countdown-label">Days</div>
                </div>
                <div class="countdown-item">
                    <div class="countdown-number" id="hours">00</div>
                    <div class="countdown-label">Hours</div>
                </div>
                <div class="countdown-item">
                    <div class="countdown-number" id="minutes">00</div>
                    <div class="countdown-label">Minutes</div>
                </div>
                <div class="countdown-item">
                    <div class="countdown-number" id="seconds">00</div>
                    <div class="countdown-label">Seconds</div>
                </div>
            </div>

            <div class="hero-cta">
                <a href="#register" class="btn-primary">
                    <span>Register Now</span>
                    <span>→</span>
                </a>
            </div>
        </div>

        <div class="scroll-indicator">
            <span class="scroll-text">Scroll</span>
            <div class="scroll-line"></div>
        </div>
    </section>

    <!-- ABOUT SECTION -->
    <section class="about" id="about">
        <div class="section-header">
            <span class="section-number">01</span>
            <div class="section-badge">About the Conference</div>
            <h2 class="section-title reveal">A Premier Platform for<br><span class="highlight">Future Diplomats</span></h2>
            <p class="section-description reveal">NNRG Model United Nations 2026 brings together brilliant minds from across the nation to engage in meaningful diplomatic discourse.</p>
        </div>

        <div class="about-content-wrapper">
            <div class="about-text reveal">
                <p>The NNRG Model United Nations Conference represents a unique convergence of academic excellence and practical diplomatic training. Organized by the prestigious Nalla Narasimha Reddy Education Society's Group of Institutions, this conference is designed to cultivate the next generation of global leaders.</p>
                
                <p>Through intensive committee sessions, expert-led workshops, and real-world simulation of United Nations proceedings, delegates will develop critical skills in negotiation, public speaking, research, and consensus-building.</p>
            </div>

            <div class="about-features reveal">
                <div class="about-feature">
                    <div class="about-feature-icon">🎯</div>
                    <div class="about-feature-text">
                        <h4>Expert Guidance</h4>
                        <p>Learn from experienced MUN professionals</p>
                    </div>
                </div>
                <div class="about-feature">
                    <div class="about-feature-icon">🌍</div>
                    <div class="about-feature-text">
                        <h4>Global Issues</h4>
                        <p>Tackle real-world challenges</p>
                    </div>
                </div>
                <div class="about-feature">
                    <div class="about-feature-icon">🏆</div>
                    <div class="about-feature-text">
                        <h4>Recognition</h4>
                        <p>Certificates & prestigious awards</p>
                    </div>
                </div>
                <div class="about-feature">
                    <div class="about-feature-icon">🤝</div>
                    <div class="about-feature-text">
                        <h4>Networking</h4>
                        <p>Connect with like-minded peers</p>
                    </div>
                </div>
            </div>

            <div class="about-stats">
                <div class="stat-card reveal stagger-1">
                    <div class="stat-number">300+</div>
                    <div class="stat-label">Delegates Expected</div>
                </div>
                <div class="stat-card reveal stagger-2">
                    <div class="stat-number">3</div>
                    <div class="stat-label">UN Committees</div>
                </div>
                <div class="stat-card reveal stagger-3">
                    <div class="stat-number">2</div>
                    <div class="stat-label">Days of Excellence</div>
                </div>
            </div>
        </div>
    </section>

    <!-- WHY NNRG MUN -->
    <section class="why-nnrg" id="why">
        <div class="section-header">
            <span class="section-number">02</span>
            <div class="section-badge">Why Choose Us</div>
            <h2 class="section-title reveal">Excellence in<br><span class="highlight">Every Aspect</span></h2>
            <p class="section-description reveal">Experience a conference that sets new standards in Model United Nations education</p>
        </div>

        <div class="features-grid">
            <div class="feature-card reveal stagger-1">
                <div class="feature-icon">🎓</div>
                <h3 class="feature-title">Academic Rigor</h3>
                <p class="feature-description">Expert faculty oversight, research-backed agendas, and scholarly approach to international relations ensure intellectual depth.</p>
            </div>
            <div class="feature-card reveal stagger-2">
                <div class="feature-icon">🏛️</div>
                <h3 class="feature-title">Institutional Excellence</h3>
                <p class="feature-description">Backed by NNRG Education Society with decades of educational leadership and commitment to student development.</p>
            </div>
            <div class="feature-card reveal stagger-3">
                <div class="feature-icon">🌟</div>
                <h3 class="feature-title">Professional Experience</h3>
                <p class="feature-description">State-of-the-art facilities, authentic UN simulation, and networking opportunities with industry professionals.</p>
            </div>
            <div class="feature-card reveal stagger-4">
                <div class="feature-icon">📜</div>
                <h3 class="feature-title">Recognized Certification</h3>
                <p class="feature-description">Official participation certificates and awards recognized by universities and organizations nationwide.</p>
            </div>
            <div class="feature-card reveal stagger-5">
                <div class="feature-icon">🤝</div>
                <h3 class="feature-title">Collaborative Learning</h3>
                <p class="feature-description">Build lasting connections with like-minded peers while developing essential diplomatic and leadership skills.</p>
            </div>
            <div class="feature-card reveal stagger-6">
                <div class="feature-icon">💎</div>
                <h3 class="feature-title">Unmatched Value</h3>
                <p class="feature-description">Premium conference experience at an accessible price point, ensuring quality education for all aspiring diplomats.</p>
            </div>
        </div>
    </section>

    <!-- COMMITTEES -->
    <section class="committees" id="committees">
        <div class="section-header">
            <span class="section-number">03</span>
            <div class="section-badge">UN Committees</div>
            <h2 class="section-title reveal">Engage in<br><span class="highlight">Global Discourse</span></h2>
            <p class="section-description reveal">Hover over each committee to discover the agenda</p>
        </div>

        <div class="committees-grid">
            <!-- UNGA -->
            <div class="committee-card reveal stagger-1">
                <div class="committee-card-inner">
                    <div class="committee-front">
                        <div class="committee-emblem">
                            <span class="committee-emblem-text">UN</span>
                        </div>
                        <h3 class="committee-name">UNGA</h3>
                        <p class="committee-full-name">United Nations General Assembly</p>
                        <div class="flip-indicator">Hover to view agenda</div>
                    </div>
                    <div class="committee-back">
                        <div class="agenda-label">Committee Agenda</div>
                        <p class="agenda-text">Addressing climate change adaptation in developing nations with focus on sustainable financing and technology transfer mechanisms.</p>
                        <div class="committee-back-footer">UNGA • 2026</div>
                    </div>
                </div>
            </div>

            <!-- UNSC -->
            <div class="committee-card reveal stagger-2">
                <div class="committee-card-inner">
                    <div class="committee-front">
                        <div class="committee-emblem">
                            <span class="committee-emblem-text">SC</span>
                        </div>
                        <h3 class="committee-name">UNSC</h3>
                        <p class="committee-full-name">United Nations Security Council</p>
                        <div class="flip-indicator">Hover to view agenda</div>
                    </div>
                    <div class="committee-back">
                        <div class="agenda-label">Committee Agenda</div>
                        <p class="agenda-text">Maintaining international peace and security in regions affected by cyber warfare and emerging security threats in the digital age.</p>
                        <div class="committee-back-footer">UNSC • 2026</div>
                    </div>
                </div>
            </div>

            <!-- WHO -->
            <div class="committee-card reveal stagger-3">
                <div class="committee-card-inner">
                    <div class="committee-front">
                        <div class="committee-emblem">
                            <span class="committee-emblem-text">WHO</span>
                        </div>
                        <h3 class="committee-name">WHO</h3>
                        <p class="committee-full-name">World Health Organization</p>
                        <div class="flip-indicator">Hover to view agenda</div>
                    </div>
                    <div class="committee-back">
                        <div class="agenda-label">Committee Agenda</div>
                        <p class="agenda-text">Strengthening global health security frameworks and pandemic preparedness in the post-COVID era with equitable access focus.</p>
                        <div class="committee-back-footer">WHO • 2026</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- TIMELINE -->
    <section class="timeline" id="timeline">
        <div class="section-header">
            <span class="section-number">04</span>
            <div class="section-badge">Conference Timeline</div>
            <h2 class="section-title reveal">Your Journey to<br><span class="highlight">Excellence</span></h2>
            <p class="section-description reveal">Key dates and milestones for NNRG MUN 2026</p>
        </div>

        <div class="timeline-container">
            <div class="timeline-line"></div>

            <div class="timeline-item">
                <div class="timeline-content">
                    <div class="timeline-date">January 15, 2026</div>
                    <h3 class="timeline-title">Registrations Open</h3>
                    <p class="timeline-description">Early bird registration begins. Secure your spot at NNRG MUN 2026.</p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item">
                <div class="timeline-content">
                    <div class="timeline-date">February 1, 2026</div>
                    <h3 class="timeline-title">Agenda Release</h3>
                    <p class="timeline-description">Committee agendas and background guides published for delegate preparation.</p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item">
                <div class="timeline-content">
                    <div class="timeline-date">February 20, 2026</div>
                    <h3 class="timeline-title">Country Allotment</h3>
                    <p class="timeline-description">Country portfolios assigned to all registered delegates via email.</p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item">
                <div class="timeline-content">
                    <div class="timeline-date">March 1, 2026</div>
                    <h3 class="timeline-title">Registration Deadline</h3>
                    <p class="timeline-description">Final date for delegate registration. Late entries may not be accommodated.</p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item">
                <div class="timeline-content">
                    <div class="timeline-date">March 15-16, 2026</div>
                    <h3 class="timeline-title">Conference Days</h3>
                    <p class="timeline-description">Two-day intensive conference with committee sessions, socials, and closing ceremony.</p>
                </div>
                <div class="timeline-dot"></div>
            </div>
        </div>
    </section>

    <!-- REGISTRATION -->
    <section class="registration" id="register">
        <div class="section-header">
            <div class="section-badge">Join Us</div>
            <h2 class="section-title reveal">Secure Your<br>Seat Today</h2>
        </div>

        <div class="registration-container reveal">
            <div class="registration-form-wrapper">
                <iframe 
                    src="https://docs.google.com/forms/d/e/1FAIpQLSd3Arl5KFEe5Fa2uGCcaTdFhIJbimoZ55gplGyJKJWhcSnBVQ/viewform?embedded=true" 
                    class="form-iframe"
                    id="registrationForm">
                    Loading form...
                </iframe>
            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer>
        <div class="footer-content">
            <div class="footer-brand">
                <h3>NNRG MUN 2026</h3>
                <p>Organized by Nalla Narasimha Reddy Education Society's Group of Institutions. Empowering students through diplomatic education and international relations training.</p>
            </div>
            <div class="footer-section">
                <h4>Quick Links</h4>
                <ul class="footer-links">
                    <li><a href="#about">About Conference</a></li>
                    <li><a href="#committees">Committees</a></li>
                    <li><a href="#timeline">Timeline</a></li>
                    <li><a href="#register">Register Now</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Connect</h4>
                <ul class="footer-links">
                    <li><a href="https://www.instagram.com/nnrgmun?igsh=MXExMHhpNXExODd5Ng==" target="_blank">Instagram</a></li>
                    <li><a href="#">LinkedIn</a></li>
                    <li><a href="#">Email Us</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Location</h4>
                <ul class="footer-links">
                    <li><a href="https://maps.app.goo.gl/QvUGkgFe4FzXqew88?g_st=ipc" target="_blank">Campus Location</a></li>
                    <li><a href="#">Hyderabad, India</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>© 2026 NNRG Model United Nations. All rights reserved.</p>
        </div>
    </footer>

    <!-- MOBILE STICKY CTA -->
    <div class="mobile-cta">
        <a href="#register" class="btn-primary">Register Now →</a>
    </div>

    <script>
        // ===== PRELOADER =====
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('preloader').classList.add('hidden');
            }, 2500);
        });

        // ===== CUSTOM CURSOR =====
        const cursor = document.getElementById('cursor');
        const cursorDot = document.getElementById('cursorDot');

        document.addEventListener('mousemove', (e) => {
            cursor.style.left = e.clientX + 'px';
            cursor.style.top = e.clientY + 'px';
            cursorDot.style.left = e.clientX + 'px';
            cursorDot.style.top = e.clientY + 'px';
        });

        document.querySelectorAll('a, button, .committee-card').forEach(el => {
            el.addEventListener('mouseenter', () => cursor.classList.add('hover'));
            el.addEventListener('mouseleave', () => cursor.classList.remove('hover'));
        });

        // ===== PARTICLES =====
        const particlesContainer = document.getElementById('particles');
        for (let i = 0; i < 30; i++) {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.left = Math.random() * 100 + '%';
            particle.style.top = Math.random() * 100 + '%';
            particle.style.animationDelay = Math.random() * 15 + 's';
            particle.style.animationDuration = (Math.random() * 10 + 10) + 's';
            particlesContainer.appendChild(particle);
        }

        // ===== NAVIGATION SCROLL =====
        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 100) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // ===== MOBILE MENU =====
        const menuToggle = document.getElementById('menuToggle');
        const mobileMenu = document.getElementById('mobileMenu');
        const menuClose = document.getElementById('menuClose');

        menuToggle.addEventListener('click', () => {
            mobileMenu.classList.add('active');
        });

        menuClose.addEventListener('click', () => {
            mobileMenu.classList.remove('active');
        });

        mobileMenu.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
                mobileMenu.classList.remove('active');
            });
        });

        // ===== SMOOTH SCROLLING =====
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // ===== COUNTDOWN TIMER =====
        const CONFIG = {
            eventDate: '2026-03-15T09:00:00'
        };

        function updateCountdown() {
            const eventDate = new Date(CONFIG.eventDate).getTime();
            const now = new Date().getTime();
            const distance = eventDate - now;

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById('days').innerText = String(days).padStart(2, '0');
            document.getElementById('hours').innerText = String(hours).padStart(2, '0');
            document.getElementById('minutes').innerText = String(minutes).padStart(2, '0');
            document.getElementById('seconds').innerText = String(seconds).padStart(2, '0');
        }

        setInterval(updateCountdown, 1000);
        updateCountdown();

        // ===== SCROLL REVEAL ANIMATIONS =====
        const revealElements = document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .timeline-item');

        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        });

        revealElements.forEach(el => revealObserver.observe(el));

        // ===== PARALLAX EFFECT =====
        document.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const gridLines = document.querySelector('.grid-lines');
            if (gridLines) {
                gridLines.style.transform = `translateY(${scrolled * 0.1}px)`;
            }
        });

        // ===== NUMBER COUNTER ANIMATION =====
        const counters = document.querySelectorAll('.stat-number');
        const counterObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const target = entry.target;
                    const value = target.innerText;
                    const numericValue = parseInt(value.replace(/\D/g, ''));
                    const suffix = value.replace(/[0-9]/g, '');
                    
                    let current = 0;
                    const increment = numericValue / 60;
                    const timer = setInterval(() => {
                        current += increment;
                        if (current >= numericValue) {
                            target.innerText = numericValue + suffix;
                            clearInterval(timer);
                        } else {
                            target.innerText = Math.floor(current) + suffix;
                        }
                    }, 30);
                    
                    counterObserver.unobserve(target);
                }
            });
        }, { threshold: 0.5 });

        counters.forEach(counter => counterObserver.observe(counter));
    </script>
</body>
</html>